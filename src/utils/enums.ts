export enum SelectMode {
    EMPTY = '',
    SINGLE = 'single',
    MULTIPLE = 'multiple',
}

export enum SelectEvent {
    EMPTY = "selectEmpty",
    SINGLE = "selectSingle",
    MULTIPLE = "selectMultiple",
}