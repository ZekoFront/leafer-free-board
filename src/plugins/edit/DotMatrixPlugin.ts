/**
 * DotMatrixPlugin — 点阵网格背景插件
 *
 * 基于 leafer-x-dot-matrix，在 ground 层绘制可配置的点阵辅助网格。
 *
 * 核心职责：
 * - 初始化 DotMatrix 并挂载到 app.ground 或 tree 背景
 * - 支持网格间距、颜色、显隐切换（可选 API）
 * - destroy 时移除网格层
 *
 * 依赖：leafer-x-dot-matrix、EditorCore.app
 *
 * 分类：plugins/edit — EditorCanvas 默认加载（可选关闭）
 *
 * 迁移来源：`src/editor/plugins/DotMatrixPlugin.ts`
 */

export class DotMatrixPlugin {}
